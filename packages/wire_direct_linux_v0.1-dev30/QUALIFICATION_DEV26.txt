Wire Direct Linux v0.1-dev26 qualification
==========================================
Runtime: ooRexx 5.3.0 r13196 (Internal Test Version)

Executable IMAP/MailReader semantic round trip:
  wire-mailreader-imap-roundtrip=PASS count=200000 window=100 identity=INBOX|4242|8738 bodyFetches=1

Regression executable qualifications:
  wire-mailreader-roundtrip=PASS identity=INBOX|4242|8738 ingress=gtk4 effects=2
  wire-ui-proxy=PASS

Static architecture qualifications:
  PASS architecture boundaries
  PASS IMAP adapter static contract
  PASS MailReader composition
  PASS compatibility controller composition-only
  PASS renderer semantic ingress
  PASS executable application model
  PASS selection projection split
  PASS semantic subject + assessment projection
  PASS builder binding shape
  PASS builder editing path
  PASS GTK demo lifecycle contract

Important defects found/fixed while making the production composition executable:
  * WireElementTarget used Object~sendWith(name, value). ooRexx sendWith requires
    the argument vector as a one-dimensional Array. It now uses
    sendWith(name, .array~of(value)).
  * The previously vendored IMAP runtime subset omitted ImapSemantics.cls even
    though ImapSession.cls requires it. The required source is now present.

Network/live-server qualification was intentionally not performed: the acceptance
uses the production Wire IMAP adapter with a deterministic session double, requiring
no credentials and no network access.
