# Preferred Packager

This candidate follows `oorexx.package/0.1` root-manifest layout and declares Foreign Runtime as a hard dependency.

It does not vendor or modify Preferred Packager, Foreign Runtime, libssh, or any optional integration.

Package identity: `oorexx-ssh / MAIN / moduleLevel 1`.
