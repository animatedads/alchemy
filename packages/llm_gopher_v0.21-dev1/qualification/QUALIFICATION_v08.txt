LLM Gopher v0.8-dev1 qualification

New surface:
- RFC-style Gopher menu rendering through `gopher selector`.
- Minimal Gopher+ machine attributes through `--plus`.
- TCP Gopher server through `gopher serve`.
- Access-controlled selectors are views over existing sphere/article/capability/rule objects; no direct ambient EXEC is introduced.
- Named task arguments added alongside positional compatibility:
  examine source --source NAME --in PATH
  edit method --class CLASS --method METHOD --in PATH --replacement FILE
  rules check --in PATH

Evidence:
PASS ALL LLM GOPHER v0.8 SELECTOR/GOPHER TESTS
PASS ALL LLM GOPHER v0.7 LANGUAGE-RULE CYCLE TESTS
PASS ALL LLM GOPHER v0.6 BOUNDED SOURCE-EDIT TESTS
PASS ALL LLM GOPHER v0.5 LLM-USABILITY TESTS
PASS ALL LLM GOPHER v0.4 IN-ZIP TESTS
PASS ALL LLM GOPHER v0.3 PAGE TESTS
PASS semantic merge/finder/proxy/fallback/search/access tests
PASS 6 command smoke tests
PASS ALL LLM GOPHER v0.2-dev1 TESTS
PASS OOREXX RUNTIME PROBE

The v0.5/v0.6 historical tests contained a package-version assertion pinned to v0.7-dev1; in this v0.8 package those assertions were updated to v0.8-dev1. Their behavioral assertions are otherwise unchanged.

Runtime probe:
/mnt/data/oorexx_runtime_530/usr/local/bin/rexx qualification/runtime_probe.rex
=> PASS OOREXX RUNTIME PROBE

Wire probe:
A real TCP client connected to 127.0.0.1, requested /sphere/oorexx, received tab-separated menu records, CRLF line termination, selectors such as /article/ref.oorexx.class, and final dot termination.
