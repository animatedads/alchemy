LLM Gopher v0.10-dev1 qualification

New surface:
- Gopher+ ASK submission remains immediate structured JSON for v0.9 compatibility.
- Each server-side form submission also registers its exact result under /result/<digest>.
- Submission JSON exposes navigation.result_selector and navigation.return_to.
- Result pages show tool status, operation status, selected result/evidence, service/fallback route, Run again and Return selectors.
- Grouped language-rule breaches are rendered together on the same result page with > CORRECT guidance.
- Result-relative rule detail selectors return to the exact originating result page.
- Result selectors are server-local views of already-produced evidence and do not convey execution authority.

Wire evidence:
- Real TCP client submitted /form/oorexx/executable.locate and received FOUND plus /result/<digest>.
- The client fetched that result as Gopher and Gopher+ and observed status, run-again and return selectors.
- Real TCP client submitted source.language.rules.evaluate against a Python fixture containing duplicate method, wildcard import and bare except.
- One result page showed all three breaches together and all three > CORRECT entries.
- The client followed /result/<id>/rule/PYTHON.EXCEPT.BARE and received a rule page with < RETURN TO RESULT pointing to the exact same /result/<id>.

Evidence:
PASS ALL LLM GOPHER v0.10 RESULT/CYCLE TESTS
PASS ALL LLM GOPHER v0.9 GOPHER+ FORM TESTS
PASS ALL LLM GOPHER v0.8 SELECTOR/GOPHER TESTS
PASS LLM GOPHER v0.8-dev2 PYTHON PORTABILITY TESTS
PASS ALL LLM GOPHER v0.7 LANGUAGE-RULE CYCLE TESTS
PASS ALL LLM GOPHER v0.6 BOUNDED SOURCE-EDIT TESTS
PASS ALL LLM GOPHER v0.5 LLM-USABILITY TESTS
PASS ALL LLM GOPHER v0.4 IN-ZIP TESTS
PASS ALL LLM GOPHER v0.3 PAGE TESTS
PASS semantic merge/finder/proxy/fallback/search/access tests
PASS 6 command smoke tests
PASS ALL LLM GOPHER v0.2-dev1 TESTS
PASS OOREXX RUNTIME PROBE

Runtime probe:
/mnt/data/oorexx_runtime_530/usr/local/bin/rexx qualification/runtime_probe.rex
=> Open Object Rexx Version 5.3.0 r13196 - Internal Test Version
=> PASS OOREXX RUNTIME PROBE
