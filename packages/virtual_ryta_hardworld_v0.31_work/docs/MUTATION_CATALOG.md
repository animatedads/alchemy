# Virtual RYTA v0.2 Mutation Catalogue

The mutation runner is not a source-code text rewriter. It mutates the executable model objects and decision conditions so that each semantic failure can be proven observable by the suite.

Current result:

```text
M1–M16 killed: 16
survivors: 0
```

See `RULES_AND_HOLES_COVERAGE_PLAN.md` for the full catalogue and detector requirement.
