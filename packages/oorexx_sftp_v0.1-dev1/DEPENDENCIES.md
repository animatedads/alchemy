# Dependencies

Hard:

* ooRexx 5.3.0 r13196 qualification baseline
* `oorexx-ssh` MAIN >= 1
* ooRexx Foreign Runtime MAIN >= 22 (hard foundation through SSH; listed explicitly because native SSH is mandatory)

Optional:

* Storage Fabric backend
* Logging v0.7
* Secret Broker via SSH authentication layer
