# Preferred Packager

This candidate follows `oorexx.package/0.1` root-manifest layout.

Package identity: `oorexx-sftp / MAIN / moduleLevel 1`.

Hard dependencies are `oorexx-ssh MAIN >= 1` and `oorexx-foreign-runtime MAIN >= 22`.  The package does not vendor or modify either dependency or Preferred Packager.
