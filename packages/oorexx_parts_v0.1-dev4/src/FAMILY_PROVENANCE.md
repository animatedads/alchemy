# Family extension provenance

This file records the local continuation added after the supplied dev4
package. `PartsFamilies.cls` constructs the existing `PartDefinition` model
and resolves every material role through `oorexx_materials_v0.1-dev3`.

No physics, manufacturing process simulation, circuit behaviour or battery
electrochemistry is implemented here.
