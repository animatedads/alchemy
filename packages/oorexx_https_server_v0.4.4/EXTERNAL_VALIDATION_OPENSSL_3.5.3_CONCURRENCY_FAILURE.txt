Independent host report received 2026-08-30 for sealed ooRexx HTTPS Server v0.3.

Passed before failure:
- Foreign Runtime v0.13.0 activation
- libffi dispatcher
- OpenSSL provider threading contract
- explicit BIO handshake bridge
- WANT_READ/WANT_WRITE retry regression
- TLS 1.2/1.3
- HTTP routes, binary echo, HEAD, pipelining
- anti-smuggling and Host validation
- TLS version floor
- clean error-log behavior

Failure:
  20 Segmentation fault

Observed during the 16 concurrent HTTPS clients test, after multiple connection resets.
Single-client TLS remained functional. The report identified the remaining defect as concurrent TLS / Foreign Runtime interaction or shared lifecycle ownership under parallel ooRexx activities.

Original host log path reported by tester:
  /tmp/oorexx-https-server-v03.log

This evidence invalidates the sealed v0.3 concurrency acceptance claim for that host class. v0.3.1 responds by serializing every libssl/libcrypto Foreign Runtime crossing through one server-wide gate and moving network readiness waits to nonblocking RxSock select().
