import test from 'node:test';import assert from 'node:assert/strict';import {parseBootstrapSeed,loadWireUIBootstrap} from '../web/bootstrap-config.js';
test('inline browser-safe bootstrap remains supported',async()=>{const seed=parseBootstrapSeed('{"moduleUrl":"/m.js","gatewayUrl":"wss://g/wire-ui","outboundQueue":"WIREUI.IN.AP","ownership":{"applicationId":"AJI","sessionId":"S","accessPointId":"AP"}}');const c=await loadWireUIBootstrap(seed,null);assert.equal(c.ownership.sessionId,'S');assert.equal(c.outboundQueue,'WIREUI.IN.AP')});
test('bootstrap URL obtains authoritative binding',async()=>{const seed={bootstrapUrl:'/wire-ui/bootstrap?token=t'};let called;const f=async(url,options)=>{called={url,options};return{ok:true,status:200,json:async()=>({moduleUrl:'/runtime/src/index.js',gatewayUrl:'wss://aji.example/wire-ui?token=t',outboundQueue:'WIREUI.IN.AJI-42',siteId:'ALL_JAPAN_INSURANCE',ownership:{applicationId:'AJI-APP',sessionId:'S-42',accessPointId:'AJI-42'}})}};const c=await loadWireUIBootstrap(seed,f);assert.equal(called.url,seed.bootstrapUrl);assert.equal(called.options.cache,'no-store');assert.equal(c.ownership.accessPointId,'AJI-42');assert.equal('bootstrapUrl' in c,false)});
