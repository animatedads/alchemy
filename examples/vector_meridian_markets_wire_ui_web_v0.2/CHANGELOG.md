# Changelog

## v0.2

- Adds Builder v0.11 semantic source `VMMWireUIDesignFixture.cls` and exact compiled release `VECTOR_MERIDIAN_MARKETS_OPERATIONS@2`.
- Adds 23 exact HUMAN_VISUAL projections covering dealing, institutional, treasury and exception workspaces.
- Adds server-authoritative operator intents for order cancel/reconcile, algo kill/resume and exception acknowledgement.
- Keeps every action out of the live browser shell; action vocabulary arrives only through the exact compiled release.
- Adds Server v0.17 regression proving rendered controls are unusable without authoritative action availability, forged actions are rejected, Federation access-point injection fails, and mismatched release provenance fails.
- Adds compiled-release Node regression and byte-for-byte Builder rebuild qualification.
- Expands the non-authoritative preview with an operator action rail, exact release identity and evidence-first exception queue.
- Retains VMM v0.11 as the independent trading/runtime baseline; this UI package does not roll VMM runtime version.

## v0.1

- First VMM browser projection shell.
- Adds VMM branding using the approved Vector Meridian geometric mark/wordmark.
- Adds runnable non-authoritative dealing/risk preview.
- Adds live Wire UI bootstrap compatible with Wire UI Server v0.17 / Builder v0.11 / JS v0.4-dev4.
- Establishes firm-risk, orders/fills, inventory/P&L, institutional-synthetics and funding/collateral presentation regions.
- Keeps Queue Fabric and VMM services authoritative; the browser has no direct VMM engine reference and no locally authored trading semantics.
