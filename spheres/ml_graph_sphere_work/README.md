# ooRexx ML Graph Gopher sphere v0.1-dev2

Authoritative continuity sphere for the separate ooRexx ML Graph component.

The sphere preserves the renderer/evidence authority boundary, polar pattern rendering inherited from ML dev9, the dev10 cylindrical temporal-pattern adapter, optional Foreign Runtime/matplotlib provider semantics, and the synthetic market cognitive-reveal demonstration.

Start with:

```text
gopher --profile oorexx-ml-graph context oorexx-ml-graph
```
