{
  "kind": "sphere",
  "id": "google-colab",
  "version": "0.1",
  "title": "Google Colab CLI and managed-compute operations",
  "access_policy": "google-colab-access",
  "observed_as_of": "2026-09-02",
  "purpose": "Teach an LLM or operator to use Google Colab as a managed ephemeral compute provider without confusing requested resources with observed resources, provider documentation with installed CLI capability, ordinary environment with secret transport, transient transport faults with deterministic workload failure, or a successful training run with a successful promotion decision."
}
