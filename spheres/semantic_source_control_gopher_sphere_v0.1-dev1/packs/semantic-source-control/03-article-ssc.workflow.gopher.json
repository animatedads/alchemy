{
  "kind": "article",
  "id": "ssc.workflow.gopher",
  "version": "0.1-dev1",
  "sphere": "semantic-source-control",
  "title": "Normal AI workflow: examine, bounded edit, semantic scan, impact, qualification, explicit acceptance",
  "summary": "The preferred AI cycle is Gopher context/symbol/source examination -> bounded stale-fenced method edit -> language rules -> SSC semantic status/diff/impact -> focused tests/package checks -> explicit reviewer acceptance. If Gopher lacks a material step, record a dogfood escape before ambient fallback.",
  "procedure": [
    "Open the active project sphere and this SSC sphere.",
    "Locate the exact declaration with source.symbol.lookup and examine the bounded source.",
    "Use source.oorexx.method.edit (or language-specific bounded editor) with expected SHA-256 fencing; preserve unrelated bytes.",
    "Run source.language.rules.evaluate.",
    "Query SSC identity/status/delta/impact through the future bounded SSC adapter; until available, record DOGFOOD.CAPABILITY_GAP before direct osc fallback.",
    "Use SSC impact plus Gopher test.impact.discover to choose qualification; SSC semantic impact is structural evidence, Gopher test impact is advisory discovery.",
    "Run package.stage.check before sealing.",
    "Acceptance is a separate reviewer/authority action against an exact candidate digest and parent source level."
  ],
  "invariants": [
    "Never regenerate an existing source file from a partial model.",
    "Never infer acceptance from a green validator, test, package check or Gopher result.",
    "Never hide a Gopher escape that qualification depended on."
  ]
}
