# MCP Project Service Gopher sphere v0.2-dev8

Authoritative engineering/continuity sphere for `oorexx_mcp_service_v0.2-dev8.zip` SHA-256 `56a38180ae1d2f712ba9afc3f9f2aa5bd528bc04a5c976510167faf6f98287d3`.

v0.2-dev8 adds optional Qualification Execution Broker v0.1-dev1 MCP integration: seven preflight/authorization/status/result tools are advertised only when a broker is injected; worker execution and private signing keys remain outside MCP. Existing deployments without a broker retain the eight project tools.

Authoritative engineering/continuity sphere for `oorexx_mcp_service_v0.2-dev7.zip` SHA-256 `0fda08557692c4011a43972a205d7e1a03a1a00bb5fb64a5469ce4a8b8322140`.

This sphere remains separate from the executable MCP service. It records HTTPS/Wire authority boundaries, connector compatibility, strict JSON wire typing, immutable request chains, authenticated ownership claim/release, first-class Gopher sphere components, Gopher checking and qualification evidence.

## v0.2-dev7

Live Claude use exposed an architectural mismatch: an authored sphere could be resolved by Gopher but was not an ownable project component, so the closest claim target was the much broader `llm_gopher` runtime. dev7 makes each Gopher sphere a first-class component. Canonical IDs use `sphere:<sphereId>` to avoid runtime collisions; aliases such as `oorexx_llm_pitfalls` resolve to `sphere:oorexx-llm-pitfalls` where unambiguous. Requests, mailboxes, unread/read evidence and claim/release apply uniformly to both runtime and sphere components.

The sealed composite catalogue contains 82 runtime/API artifact entries and 53 sphere artifact entries (135 entries, 129 unique canonical component IDs). The source projections remain separate and reproducibly composed.

Grok live qualification also confirmed the preceding dev6 xAI fix: the compatibility connector exposed all eight tools and successfully executed component/request list calls.
