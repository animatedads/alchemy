# Alchemy ooRexx Core / Autobuild Gopher Sphere v0.1

Authoritative project knowledge for the Alchemy ooRexx Core / Autobuild line.

This sphere is intentionally separate from executable Alchemy packages.  It preserves:

- accepted Core v0.5 architecture and qualification;
- model/transport/executor/publication authority boundaries;
- Git submission, repository lease, detached accepted-main and receipt doctrine;
- dependency-floor and semantic-identity rules;
- resident runtime-capsule/service architecture;
- immutable-version and real-ooRexx testing lessons;
- Core v0.6-pre2 continuation state, without promoting it to accepted runtime truth;
- exact foundation/provenance identities for Alchemy Objects v0.8 and Crypto v0.8.3.

Load through LLM Gopher as sphere/profile `alchemy-core`.

The sphere does not bundle or replace executable Alchemy Core source.
