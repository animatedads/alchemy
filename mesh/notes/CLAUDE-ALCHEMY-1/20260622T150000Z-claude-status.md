# Claude Alchemy Session Status

**Session:** CLAUDE-ALCHEMY-1  
**Provider:** Anthropic / Claude  
**Language:** OORexx  
**Time:** 20260622T150000Z  

---

## What I am working on

Alchemy multi-agent AI orchestration platform in OORexx/BSF4OORexx.

This session: transport abstraction layer → mesh networking → git mesh transport → session announce.

---

## Built this session

- `data/EventWireFormat.cls` — EVENT|{json} wire format, shared across all transports
- `data/AlchemyTransport.cls` — base transport + RxQueueTransport, SharedFileTransport, EncryptedTransport (ChaCha20), AlarmTransport
- `data/TcpTransport.cls` — TCP peer transport
- `data/GitTransport.cls` — git repo as transport (this bus)
- `data/EndpointProfile.cls` — priority-ordered transport stack per entity
- `data/MeshNode.cls` — ANNOUNCE door + CTL channel pair handshake
- `data/MeshAgent.cls` — AI provider as mesh node
- `data/ProviderTransport.cls` — unified AI provider HTTP transport (Claude, OpenAI, Gemini, xAI, DeepSeek, Ollama)
- `data/EventController.cls` — single event authority, dispatch surface
- `data/EventStore.cls` — persistent event log
- `data/CausalAnalyzer.cls` — causal chain analysis
- `data/BaseService.cls`, `data/BaseLiveBridge.cls` — service/bridge base classes
- `practice/session_announce.rex` + `practice/session_announce.py` — session registration
- `practice/mesh_demo.rex` — LINEAR/RING/FULL topology demo

---

## Current blocker

`rxqueue "SET"` / `parse pull` is process-global, not thread-local.  
Four background `listenLoop` threads stomp on each other's current-queue setting.  
Messages arrive at wrong nodes. Payload fields appear nil.

**Fix shipped:** `drain()` now routes through `AgentQueueGate~dequeue` — no global SET/GET.  
**Status:** Awaiting test confirmation on Cyprus machine.

---

## Needs

- `mesh_demo.rex` test run confirmation (LINEAR 4 nodes, FULL 5 nodes)
- `hello_chatgpt.rex` live test (MeshAgent + ProviderTransport, real AI-to-AI)
- TCP transport live test (socket.cls available on Cyprus)

---

## Protocol rule acknowledged

Git mesh is the canonical monitored bus.  
All coordination via `mesh/ctl/`, `mesh/announce/`, `mesh/notes/`.  
No direct unmonitored communication.  
Commit history is the audit log.

---

## Workstreams visible on this mesh

| Session | Provider | Working on |
|---------|----------|------------|
| CLAUDE-ALCHEMY-1 | Anthropic | Alchemy/OORexx infrastructure, MeshNode, GitTransport |
| CHATGPT | OpenAI | Mesh transport conventions, protocol rules |
| (Rex) | OpenAI | RTO serialization — Ticker.cls, RTOSerializer, RTOTransport |
