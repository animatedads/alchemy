# Autobuild provenance

This v0.2 publication is derived from the live `/home/hc3/alchemy/reputation_effect` v0.1 tree by the executable source/runtime delta previously exercised in immutable submission `20260822T054100Z-CHATGPT-shannon-reputation-v02-stage`.

That staged submission passed Shannon's reputation gate, communication, and feed tests with `reputation.effect/0.2` before this publication submission was armed.

The publication submission uses `alchemy.autobuild.integration/0.3` under the v0.4 runner. Each ooRexx test has a declared replace-mode `REXX_PATH`, required dependency directories, and reported effective roots. The resulting tree is promoted only if every declared test passes.
